<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
<ImageView
    android:id="@+id/img_sura"
    android:layout_width="100dp"
    android:layout_height="100dp"
    android:src="@drawable/item_quraan_name_bg"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    android:layout_margin="5dp"/>

    <TextView
        android:id="@+id/sura_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/img_sura"
        app:layout_constraintEnd_toEndOf="@id/img_sura"
        app:layout_constraintStart_toStartOf="@id/img_sura"
        app:layout_constraintTop_toTopOf="@id/img_sura"
        tools:text="الفاتحة"
        android:textSize="16sp"
        android:textColor="#46352b"
         />
</androidx.constraintlayout.widget.ConstraintLayout>